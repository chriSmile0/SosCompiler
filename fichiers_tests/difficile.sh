lecture_valeurs() # Saisie des valeurs entières
{
	local i=0 ; local nombre = $1 ; #c'est que a>b ou a > b, d'où a &&b ! 
	local b=0;# on initialise b
	#c'est que a>b ou a > b, d'où a &&b ! 

	return 0
};